apiVersion: 1

providers:
  - name: Infrastructure
    orgId: 1
    folder: Infrastructure
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 10

    options:
      path: /var/lib/grafana/dashboards